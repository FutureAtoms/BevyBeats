package com.futureatoms.bevybeats.data.manager

import android.content.Context
import com.futureatoms.bevybeats.data.manager.base.BaseManager

class LoadMoreManager(
    private val context: Context,
) : BaseManager(context)